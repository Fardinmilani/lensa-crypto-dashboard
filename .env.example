# Comma-separated list of deployed Cloudflare Worker proxy URLs (no trailing
# slash). Optional -- if empty, the app fetches exchanges directly from the
# browser exactly as before. See cloudflare-proxy/README.md.
VITE_MARKET_PROXY_ENDPOINTS=https://lensa-market-proxy.YOUR-SUBDOMAIN.workers.dev
